// Copied code from project 2, will edit to work with this project.

var express = require('express');
var app = express();

app.get('/', function (req, res) {
    // res.render("login", {layout : );
    res.render("main");
});

// Da Main Boi
app.get('/dashboard', function (req, res) {
    // res.render("dashboard", {layout : ""});
    res.render("dashboard");
});

app.listen(8000, function () {
    console.log('Listening to Port 8000');
});

///  Old Code I might scrap
// //Declare the installed framesworks
// const express = require('express');
// const bodyParser = require('body-parser');
// //call the express and Body-parser
// let app = express();
// app.use(bodyParser.urlencoded({
// extended: true
// }));
// //we installed the ejs and created a file inside the views
// app.set('view engine', 'ejs');